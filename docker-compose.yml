services:
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: watodotoday
      POSTGRES_USER: watodo
      POSTGRES_PASSWORD: watodo
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
